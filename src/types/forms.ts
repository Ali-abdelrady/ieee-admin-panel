export interface SocialLinksType {
  url: string;
  icon: string;
  name: string;
}
export interface FormFiledsType {
  label: string;
  type: string;
  name: string;
  required: boolean;
  options?: string[];
}
